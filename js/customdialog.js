// Shows dialog box, while unable to interact with rest of page
export function showDialog(dialogElement) {
    dialogElement.showModal();
};

// Closes dialog box
export function closeDialog(dialogElement) {
    dialogElement.close();
}


